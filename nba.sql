CREATE TABLE nba (
	PLAYER VARCHAR NOT NULL, 
	TEAM VARCHAR NOT NULL,
	AGE INT NOT NULL,
	SEASON VARCHAR NOT NULL,
	GP INT NOT NULL,
	W INT NOT NULL, 
	L INT NOT NULL,
	MINS INT NOT NULL,
	PTS INT NOT NULL,
	FGM INT NOT NULL,
	FGA INT NOT NULL,
	FGP INT NOT NULL,
	TPM INT NOT NULL,
	TPA INT NOT NULL,
	TPP INT NOT NULL, 
	FTM INT NOT NULL,
	FTA INT NOT NULL,
	FTP INT NOT NULL, 
	OREB INT NOT NULL,
	DREB INT NOT NULL,
	REB INT NOT NULL, 
	AST INT NOT NULL,
	TOV INT NOT NULL, 
	STL INT NOT NULL,
	BLK INT NOT NULL,
	PF INT NOT NULL,
	FP INT NOT NULL,
	DD2 INT NOT NULL,
	TD3 INT NOT NULL,
	PON INT NOT NULL,
	RANKINGS INT NOT NULL
	);
SELECT * FROM nba